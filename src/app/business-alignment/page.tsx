// Business Alignment — the 12-Domain Business Alignment view.
// This is the former default dashboard, repositioned as the strategic
// "why it needs attention" layer beneath the Executive Home.
// Kept as a thin re-export so /dashboard and /business-alignment stay in sync
// during the phase-1 merge; the underlying view can be renamed later.
export { default } from "../dashboard/page";
