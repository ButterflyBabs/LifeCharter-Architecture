// What a client's AI assistant is called until they name it themselves
// (Settings → AI Assistant). One place, so changing it changes it everywhere.
export const DEFAULT_ASSISTANT_NAME = "Mariposa";

export const MAX_ASSISTANT_INSTRUCTIONS = 1500;
